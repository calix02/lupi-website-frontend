export default function Announcement(){
    return(
        <section id="announcements" className = "w-full h-screen flex items-center justify-center bg-linear-to-b from-red-500 to-red-700">
        
        </section>

    );
}